
## 项目要求
- 此项目处于开发早期阶段, 无需考虑向后兼容, 可以激进重构
- 请确保修改功能后不要留下不合适的命名
	- 例如当 更新页面 重构为 设置页面后, 及时复用 更新页面 已有的代码也要同步修改代码中的命名
- 显示错误处理, 不要使用 `?.` 等方法隐藏错误
	- 前端不要处理不可能出现的错误, 如 dom 元素缺失
- 请确保所有中文文案/错误消息使用半角标点符号
- Git 提交使用中文描述, 参考 5 个历史提交

## 修改与重构
- 对于不涉及业务代码和功能的需求, 使用修改, 比如文案修改
- 对于涉及功能改动的需求, 使用重构, 比如错误修复 / 对齐需求
- 设计配置项必须约束范围并同步修改前端

## 守护进程
修改守护进程代码前需要用户确认.

## HTML 片段
- 对于不可确定的文本, 不可使用 HTML 字符串拼接, 应使用 JS 设置文本内容

## 前端卡片与分页
- 前端大量使用弹窗卡片与分页设计来承载内容
- 参考已有卡片 `modal-card` 和过滤器 `filter-group` 以及当前已有卡片的布局来实现新卡片/页面
- 如果一个卡片页面的的最后一项 (底部操作按钮的上一项) 是输入框或任何自带边框的元素, 则不添加底部边框横线; 如果是文字, 则使用 `file-action-static` 设置底部边框横线

## GIT 提交
- 不要提交 `*_test.go` 测试文件
